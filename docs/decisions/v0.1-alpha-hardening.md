# DSHelm v0.1-alpha Hardening — MASTER TASK CONTRACT

> Status: ACTIVE · Round 3 (INTERRUPTED RECOVERY + rename round)
> Updated: recovery session · Branch: `work/deephelm-bootstrap` (worktree `deephelm-worktrees/bootstrap`)
> **This file is the master objective. Every stage, subgoal, session resume, or subagent
> completion must re-read this file first. Stage goals never override this contract.**

---

## 0. INTERRUPTED RECOVERY (Round 3 start — this session)

A previous hardening round was interrupted by a DSH rc.6 WebUI freeze. All work was
recovered from disk; nothing was lost.

| Item | Recovered value |
|---|---|
| Implementation worktree | `/home/altair/projects/agent-harness-lab/deephelm-worktrees/bootstrap` |
| Branch / HEAD | `work/deephelm-bootstrap` @ `1de3aac` (pushed: origin matches) |
| Main repo | `/home/altair/projects/agent-harness-lab/deephelm` @ `866677d` (`main`; 2 local commits ahead of origin — mux-flood patch, preset-mount guard; **never modified by this round**) |
| Uncommitted hardening work | 10 modified files + 11 untracked files (see below) |
| Recovery evidence | `/tmp/dshelm-recovery-2026-08-15T06-46-08-858Z/` (git-status, git-diff, git-diff-staged, untracked list, main-repo status) |
| Environment | `dsh --version` = `0.1.0-rc.6`; global hotfix patches present in main repo `patches/dsh-mux-flood` + `patches/dsh-preset-mount-guard` |

Uncommitted at recovery time (interrupted session wrote but did NOT commit):

- Modified: `package.json`, `packages/core/src/{config,contracts,resolver}.ts`,
  `packages/core/tests/{config-loader,policy-errors,policy-resolver}.test.ts`,
  `packages/dsh/package.json`, `packages/dsh/src/index.ts`, `pnpm-lock.yaml`
- Untracked: `docs/decisions/v0.1-alpha-hardening.md` (this file),
  `packages/core/tests/{config-validation,core-property}.test.ts`,
  `packages/dsh/src/{capabilities,config-files,model-selection,projection,provider,service,session-events,slice}.ts`

First compile of the recovered code (this session) failed with 16 type errors —
expected for "just written, never compiled" code. All are API-contract fixes
(settings section wiring, event-map augmentation target, zod schema typing,
@types/node, noUncheckedIndexedAccess), NOT redesigns. See §3.1.

**Rename decision (this round):** the product is now **DSHelm** (formerly DeepHelm).
See `docs/decisions/rename-deephelm-to-dshelm.md`. Local disk paths
(`deephelm-worktrees`, main repo dir name) intentionally keep their old names
(§9 of the rename brief); only code/package/docs/config identifiers move.

---

## 1. Total objective

Fix every external-audit contract issue found in the bootstrap round, migrate the
product to DSHelm, establish a credible test baseline, and prove a **real**
DeepSeek Harness vertical slice:

```text
DSHelm policy (runtime-validated, deterministic, traceable)
  → DSH public seams (llm runtime, agent factory, model selection, subagents, session projection)
  → real request/header with provider/model/reasoningEffort
  → ResolutionTrace == DSH effective request config
```

All work happens on the implementation branch `work/deephelm-bootstrap`. **`main` is never modified.**

## 1.1 Product definition (corrected this round)

**DSHelm Project** = an OmO-inspired, batteries-included agent distribution for
DeepSeek Harness. **DSHelm Core** = policy / configuration / routing kernel
(roles, categories, model profiles, deterministic resolution, runtime capability
validation, provenance traces, configuration/observability surfaces).

DSHelm is NOT a DSH fork, NOT another agent loop, NOT another AgentTeams, NOT a
700-plugin collection. Execution is COMPOSED from DSH-native primitives and mature
ecosystem capabilities (dsh-agent-teams, workflow plugins, fallback/router plugins)
wherever they exist. A feature visible to users (e.g. Teams) is not proof DSHelm
must reimplement its runtime.

Community reuse rule (before any new capability):
1. DSH core already provides it? 2. Mature DSH plugin? 3. AgentTeams/workflow/fallback/router?
4. Oh-My-DSH (capability catalog / research corpus / reference only — never wholesale dependency)?
5. Only a real gap justifies own implementation. Do NOT reimplement mailbox/team state;
   DSHelm policy → AgentTeams adapter/backend is the direction.

## 2. Baseline facts (re-verified at start of this round)

| Item | Value |
|---|---|
| Repo | github.com/Altairpaca/deephelm → **github.com/Altairpaca/dshelm (rename target)** (git root = `agent-harness-lab/deephelm`) |
| Implementation branch | `work/deephelm-bootstrap` @ `1de3aac` (recovered HEAD) |
| Working tree | `agent-harness-lab/deephelm-worktrees/bootstrap` (path kept for now) |
| Pinned DSH source checkout | `agent-harness-lab/deepseek-harness` @ `47f943859bef60e4160492346772ded9b24f765a` (rc.5) — read-only reference |
| Installed dsh CLI | `0.1.0-rc.6` (global bun install) — **do NOT reinstall globally** (would wipe the mux-flood hotfix; node-pty rebuild risk) |
| Published DSH packages | `@deepseek-ai/dsh-*` `0.1.0-rc.6` (workspace deps; hermetic lane) |
| npm availability | `@dshelm` scope: **to be verified before publish** (Stage 6); if unavailable → recorded blocker, product name unchanged |
| Cordis | `@deepseek-ai/cordis` `4.0.1` |
| node / pnpm | node ≥22.19 (engines), pnpm 11.7.0 |

### 3.1 Recovered-code compile errors (all fixed in Stage 1)

1. `capabilities.ts`: spread of possibly-undefined provider entry made `enabled` optional
   (exactOptionalPropertyTypes) — guard before spread.
2. `config-files.ts` / `index.ts` / `slice.ts`: `node:*` and `process` unresolved —
   add `@types/node` to `packages/dsh` devDependencies.
3. `config-files.ts`: `installSettingsSection` signature is
   `(ctx, ns, schema: z<T>, entry: T, hooks)` with `setSource(current: () => T)` — a thunk,
   not a value; index.ts passed a 3rd arg that does not exist → wire through the returned reader.
4. `index.ts`: `ctx.sessions.get` requires branded `SessionId` — wrap the string.
5. `session-events.ts`: augmentation must target `'@deepseek-ai/dsh-session/types'`
   (root index does not re-export the `SessionEventMap` interface from the augmented module).
6. `projection.ts`: `ProjectionDefinition.schema` is `ZodType<SessionProjectionMap[K]>`;
   zod `.optional()` output (`T | undefined`) mismatches exact-optional interfaces → type the
   schema as `z.ZodType<ControlPlaneProjectionValue>` (variance-only cast; runtime `parse`
   stays the gate).
7. `slice.ts`: `noUncheckedIndexedAccess` — guard `items[index]` before use.

## 3. DSH seam evidence (authority = pinned checkout source; quoted doc lines)

| Seam | Evidence (file @ 47f943859b) |
|---|---|
| `ctx.llm.listModels` advisory | `packages/llm/llm/src/index.ts`: "Catalog membership is advisory and never changes routing or request validation." |
| `ctx.llm.resolveModelInfo(provider, model, signal)` | `packages/llm/llm/src/index.ts`: exact-route metadata incl. `reasoning.efforts[].id`, `defaultEffort`, context, defaultMaxTokens; throws `NO_ADAPTER` for unregistered provider. |
| `ctx.llm.resolveCallConfig` / `prepareCall` | rejects unsupported effort with `UNSUPPORTED_REASONING_EFFORT`; materializes adapter defaults; returns deep-frozen config. |
| ReasoningEffortId | `packages/llm/llm/src/brand.ts` (Branded<string>); deepseek adapter efforts `off|high|max` (`packages/llm/llm-deepseek/src/index.ts`, `adapter.ts`). |
| `GenerateOptions` | `packages/llm/llm/src/types.ts`: `provider/model/reasoningEffort/messages/...` — the actual adapter request. |
| `LlmCallConfig` | `packages/llm/llm/src/call-config.ts`: provider/model/reasoningEffort/temperature/maxTokens/stop — the logged request header. |
| Request header logging | `packages/core/agent-loop/src/agent.ts` `buildRequest()`: `session.append('request/header', { header, reason })`; `GenerateOptions` built from `header.config`. |
| Model selection composition | `packages/core/agent/src/model-selection.ts`: `installModelSelection(agentCtx, ModelSelectionRef)` hooks `system-prompt/assemble` + `agent/request` → provider/model/reasoningEffort into request config. Official call sites: `packages/bundle/headless/src/index.ts`, `packages/host/apiproxy/src/api-proxy.ts`. |
| Agent factory | `packages/core/agent/src/index.ts` `AgentRegistry.create({ sessionId, meta, agentOptions, setup })`; setup composes before publication. |
| SubagentStartRequest | `packages/subagent/subagent/src/types.ts`: `outputSchema/maxDepth/toolFilter/persona` + `SubagentCapabilities`; `SubagentRuntime.start` rejects missing capability with `UNSUPPORTED_CAPABILITY`. |
| In-process spawn driver | `packages/subagent/subagent-in-process-driver/src/index.ts` `startInProcessRun(request, { seed })`; provider registration pattern in `subagent-spawn-in-process`. |
| Cordis provide | `vendor/cordis/src/reflect.ts`: duplicate service in same scope throws `service "<name>" has been registered at <fiber>`; disposer unregisters; fiber-owned lifecycle. |
| Session projections (host→client) | `packages/session/session-projection/src/index.ts` `ProjectionDefinition{key,schema,init,apply,view,stateVersion}`, merge-extensible `SessionProjectionMap`; wire `session/projection` frames (`packages/host/apiproxy/src/api/events.ts`); client `useProjection` (`packages/client/runtime/src/client/sessions/projection-store.ts`). |
| Client slots | `packages/client/ui-slots/src/index.ts` SlotMap; session-scope additive seats e.g. `conversation.session.header.actions`, `conversation.input.dock`, `conversation.composer.dock` (`ui-conversation/src/client/contract/slots.ts`). Client plugin build: `packages/client/tsdown.client.ts` (closure-factory `__ModuleLoader__.load`, externals = platform module table). |
| RPC surface is fixed | `packages/host/apiproxy/src/api/index.ts` + `rpc-map.ts`: no plugin RPC extension point → DSHelm host→client channel uses session events + session projections (official), NOT new RPC methods. |
| Settings seam | `@deepseek-ai/dsh-settings`: `installSettingsSection(ctx, ns, schema, entry, hooks)`, `settingsNamespace(name)`, `SettingsSectionHooks.setSource(current: () => T)`. |
| Event-map augmentation | `SessionEventMap` lives in `@deepseek-ai/dsh-session/types` (lib/types/types.d.ts); augment THAT submodule (official pattern: `subagent/src/descriptor.ts`). |

## 4. P0 / P1 checklist

### P0 (contract correctness — must all be GREEN)
- [ ] Runtime policy schema validation: nested, per-entity, references, non-empty, key/id consistency, unknown-field policy, plain-JSON output.
- [ ] Prototype-pollution guard (`__proto__`, `prototype`, `constructor` rejected) — config AND resolver inputs.
- [ ] Opaque reasoning: core treats reasoning as validated string; no `low|medium|high` vocabulary.
- [ ] Reasoning enters real DSH request: resolved provider/model/reasoningEffort appear in session `request/header` config and adapter `GenerateOptions`.
- [ ] `listModels` no longer a whitelist; exact-model validation via `resolveModelInfo`; dynamic/unlisted-but-valid model accepted.
- [ ] Per-candidate evaluation with structured reasons: selected | provider-unknown | provider-disabled | model-invalid | model-unresolved | reasoning-unsupported | capability-mismatch; aggregate error derives from the trace.
- [ ] `inherits` semantic fixed: removed (v0.1 choice — no real use case) with explicit validation error.
- [ ] Tool/persona/depth policy → official SubagentStartRequest mapping; unsupported provider ⇒ fail loud (`UNSUPPORTED_CAPABILITY`).
- [ ] Real Cordis host service (`dshelm.policy`): resolve/snapshot/explain; provided by DSHelm bundle; fiber lifecycle; duplicate registration fails; loader composition verified.
- [ ] `.dshelm/config.jsonc` project config + `.dshelm/local/` ignored; precedence defaults→user→project→request→runtime validation, tested.
- [ ] Host→client transport: session events + session projection (official), no fake service.
- [ ] DSH-native client slot registration; renderer smoke (qa:web-renderer) split from real DSH Web QA (qa:dsh-web).
- [ ] Hermetic default tests (npm public deps); optional DSH_REFERENCE_DIR source lane.
- [ ] Manifest: remove `dshClient`; `dsh.client` authority; every export exists; pack + fresh-install verified.

### P1 (slice proof, CI, docs)
- [ ] Real Planner→Workers→Reviewer dataflow: Goal→Planner→PlanArtifact→bounded parallel Workers→WorkerResults→deterministic gates→Reviewer verdict (PASS | REVISE+evidence)→bounded revision (maxIterations, no infinite loops).
- [ ] Keyless real-execution assertion: resolved policy → actual DSH request configuration (real agent loop + real LlmRuntime + test adapter; request/header == ResolutionTrace).
- [ ] Credentialed E2E when a valid DeepSeek credential exists; otherwise record blocker (non-blocking).
- [ ] CI: core-unit, core-property, typecheck, build, pack-install, dsh-contract, client-jsdom, dsh-web-keyless, dsh-upstream-compat (informational).
- [ ] Compatibility metadata: supported DSH version + tested commit.
- [ ] Core test baseline + property/fuzz + determinism invariant + performance baseline (small/100/1000, p50/p95/p99, memory, trace size).
- [ ] Conversation Import ADR (optional plugin capability; INDEX/ARCHIVE/CONTINUE; source safety; adapter interfaces) — no importer implementation this round unless all P0/P1 above are GREEN.
- [ ] README honesty: no "Verified v0.1 vertical slice" until real host service, real Web slot, real dataflow, and request-header verification are done; then "v0.1-alpha vertical slice verified" with per-claim evidence pointers.

## 5. Acceptance criteria (final)

Every item in §4 P0 must be GREEN with test evidence; P1 items as marked. Full final report must list:
completed items, uncompleted items, per-criterion evidence, test/build/QA results, actual
DSH model/reasoning trace, commit list, blockers, next-stage importer recommendations.
Declare completion ONLY when the §4 checklist is satisfied or an external blocker is recorded.

## 6. Verified / unverified ledger

| Date | Item | Result | Evidence |
|---|---|---|---|
| recovery | Interrupted work recovered from disk | VERIFIED — nothing lost | /tmp/dshelm-recovery-2026-08-15T06-46-08-858Z/ |
| recovery | Active worktree identified | VERIFIED — deephelm-worktrees/bootstrap @ 1de3aac | git worktree list |
| recovery | DSH CLI version | VERIFIED 0.1.0-rc.6 | dsh --version |
| recovery | mux-flood hotfix present on disk | VERIFIED (patches/dsh-mux-flood + dsh-preset-mount-guard in main repo, committed 866677d) | git log main |
| start | Pinned checkout == task baseline | VERIFIED (47f943859b rc.5) | git log above |
| start | Installed CLI drift | RECORDED (rc.6, seam APIs identical) | installed lib/*.d.ts compared |
| start | All §3 seams read from source | VERIFIED | §3 table |
| round3 | Recovered code first compile | FAILED 16 errors → ALL FIXED in Stage 1 (API-contract only) | §3.1 |
| round3 | Stage 0 commit | DONE 23a0d9f (pushed) | git log |
| round3 | Stage 1 rename + compile + tests | DONE 97df475 (pushed, remote SHA verified) — typecheck/build green, 49/49 tests | git log, pnpm typecheck/test/build |

## 7. Stage commits (append as they land)

| Stage | Commit | Content |
|---|---|---|
| 0 | 23a0d9f | docs: recovery evidence + rename ADR + master contract update |
| 1 | 97df475 | feat: DeepHelm → DSHelm semantic rename (code/package/docs/config), compile green, 49/49 tests |
| 2 | (pending) | fix(core): harden policy validation, reasoning contract, candidate traces, inheritance removal |
| 3 | (pending) | fix(dsh): align model resolution with DSH runtime (resolveModelInfo); reasoning into real request config |
| 4 | (pending) | feat(dsh): real DSHelm Cordis host service + config precedence + projection/client transport |
| 5 | (pending) | feat: verified heterogeneous execution slice (planner→workers→reviewer dataflow) + request-header proof |
| 6 | (pending) | test: hermetic DSH contract suite + CI + packaging gates; perf baseline |
| 7 | (pending) | docs: conversation import ADR; README status |

## 8. Blockers

- None recorded yet. (Credentialed E2E may be blocked by missing DeepSeek credential — non-blocking for P0.)
- npm `@dshelm` scope availability — to be checked at Stage 6 (if unavailable: recorded blocker, product name stays DSHelm).
